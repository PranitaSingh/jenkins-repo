#!groovy
pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                script{
                    def output = sh(script:'date', returnStdout: true)
                    echo "OUTPUT:${output}" 
                    }
            }
        }
    } 
}
